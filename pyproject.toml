[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "kuramoto-app"
version = "0.1.0"
description = "Streamlit app for visualizing the Kuramoto model."
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.44.1",
    "ipython>=9.1.0",
    "matplotlib>=3.10.1",
    "networkx>=3.4.2",
    "numpy>=2.2.4",
    "pandas>=2.2.3",
    "plotly>=6.0.1",
    "scipy>=1.15.2",
    "sqlalchemy>=2.0.40",
    "trafilatura>=2.0.0",
]

[project.optional-dependencies]
dev = ["pytest", "ruff", "mypy", "pre-commit", "ipykernel"]

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["kuramoto"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
